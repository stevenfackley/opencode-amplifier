---
description: Design + build a distinctive UI (designer agent) — commits to a bold aesthetic, avoids generic AI slop
agent: designer
subtask: true
---

Follow the `distinctive-ui-design` discipline. Before any code, state the committed direction
(purpose/audience, an EXTREME tone, the one memorable differentiator). Then implement
production-grade UI built on a token system, pulling the levers hard (distinctive type, dominant
color + sharp accent, one orchestrated page-load, asymmetric composition, atmospheric
background). Run the anti-slop checklist before finishing. Honor any existing brand/design system.

Brief:
$ARGUMENTS
