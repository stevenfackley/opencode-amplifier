---
description: Debug a failure using the disciplined reproduce->isolate->fix->verify protocol
agent: debugger
subtask: true
---

Follow the debugging protocol in your system prompt exactly — reproduce first, one hypothesis
at a time, evidence before fixes, verify with a regression test at the end.

Symptom / failure:
$ARGUMENTS
