---
description: Pull a golden pattern into context so the model adapts it instead of inventing
---

Load this golden pattern and its header (when to use / key decisions / gotchas):

@patterns/$1/README.md

Study the pattern, then apply it to the current task by **adapting** it — match its structure
and decisions; do not reinvent a weaker version. If the pattern doesn't actually fit, say so
instead of forcing it.

Additional context: $ARGUMENTS
