# Secrets — never commit proxy keys
.env
.env.*
*.key

# Node (MCP servers pulled via npx may cache here)
node_modules/

# OpenCode local state
.opencode/cache/
.opencode/logs/

# OS
.DS_Store
Thumbs.db
